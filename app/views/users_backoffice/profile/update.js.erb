document.querySelectorAll("#imgAvatarNav").forEach((el) => 
   el.src = "<%= url_for(@user.user_profile.avatar) %>"
);

document.querySelectorAll("#imgAvatarLg").forEach((el) => 
   el.src = "<%= url_for(@user.user_profile.avatar) %>"
);

$('#avatarModal').modal('hide');

$.bootstrapGrowl("Avatar atualizado com sucesso!", {
   type: 'success', // (null, 'info', 'danger', 'success')
   align: 'right', // ('left', 'right', or 'center')
   allow_dismiss: true, // If true then will display a cross to close the popup.
   offset: {from: 'top', amount: 90}, // 'top', or 'bottom'
   width: 300, // (integer, or 'auto')
   stackup_spacing: 10 // spacing between consecutively stacked growls.
}); 